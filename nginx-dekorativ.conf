# Nginx configuration for Dekorativ
# Place at: /etc/nginx/sites-available/dekorativ
# Symlink: ln -s /etc/nginx/sites-available/dekorativ /etc/nginx/sites-enabled/

server {
    listen 80;
    listen [::]:80;
    server_name xn--e1afkbcddccmfb4k.xn--p1ai;  # декоратив.рф (Punycode)

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
